<%= render 'shared/errors', obj: @list %>
 <%= form_for [@user, @list], method: :patch do |f| %>
   <%= f.label :title %>
   <%= f.text_field :title %>
   <br/>
   <%= f.submit "Create List" %>
<% end %>